({
    /*fetchPicklistFields : function(cmp) {
        var pklstflds = component.get("v.picklistFields");
        var AccountPicklistFields = ["name"];
        pklstflds['Account'] = AccountPicklistFields
        this.getPicklistValues(component,pklstflds);
    },
    getPicklistValues : function(component, sobjFieldsmap) {
        var action = component.get("c.getPicklistValues");
        action.setParams({
            "objpicklistFieldsMap": JSON.stringify(sobjFieldsmap)
        });
        action.setCallback(this, function(resp) {
        var state=resp.getState();

        }

    } */
    })